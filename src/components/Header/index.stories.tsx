import React from 'react';
import { Header } from './index';

export default {
  title: 'components/Header',
  component: Header,
};

const Primary = (): JSX.Element => <Header />;

export { Primary };
